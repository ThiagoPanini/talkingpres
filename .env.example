# .env.example — copie para .env e preencha. NUNCA commite o .env (está no .gitignore).
#
# O MCP oficial da Hostinger lê este arquivo diretamente via dotenv. O token Coolify NÃO é
# lido daqui: o `.mcp.json` versionado traz `${COOLIFY_ACCESS_TOKEN}`, e essa expansão lê o
# ambiente do processo. Exporte-o no shell antes de abrir o agente. Setup: docs/agents/mcps.md.

# --- Coolify (MCP de deploy) ---
# Exportado no shell, não lido deste arquivo. Gere em: vps.panlabs.tech -> Keys & Tokens.
#   export COOLIFY_ACCESS_TOKEN=...
# Opcional, só para apontar para outro Coolify (default: https://vps.panlabs.tech):
#   export COOLIFY_BASE_URL=...

# --- Hostinger API (MCP de gerência da VPS) ---
# Lido diretamente pelo MCP `hostinger-api-mcp` (binário `hostinger-vps-mcp`) via dotenv.
# O pacote oficial reconhece, nesta ordem: HOSTINGER_API_TOKEN > API_TOKEN > APITOKEN.
# Gere em: hPanel -> Conta -> API  (https://hpanel.hostinger.com/profile/api).
# Trate como segredo (acesso total à VPS via API). Rotacione imediatamente se vazar.
# Setup: docs/agents/mcps.md
HOSTINGER_API_TOKEN=

# --- Cloudflare API (MCP remoto de DNS/edge) ---
# OPCIONAL e não-wired por padrão. O caminho padrão é OAuth: conecte pelo cliente (ex.: /mcp
# no Claude Code) e autorize no browser — nada vai no .env. Preencha abaixo SÓ para modo
# headless/CI, passando o token como Bearer via header/bearer_token_env_var no cliente.
# Gere um API token escopado em: dash.cloudflare.com -> My Profile -> API Tokens.
# Setup: docs/agents/mcps.md
CLOUDFLARE_API_TOKEN=

# --- OAuth Social (E0c) — GitHub / Google ---
# Instruções para o operador (🔴 segredo emitido por terceiro):
#
# GitHub OAuth App:
#   1. github.com -> Settings -> Developer settings -> OAuth Apps -> New OAuth App
#   2. Homepage URL: https://ethitorial.panlabs.tech (ou seu domínio)
#   3. Authorization callback URL: https://ethitorial.panlabs.tech/api/auth/callback/github
#      (local: http://localhost:3000/api/auth/callback/github)
#   4. Copie o Client ID e gere um Client Secret
#   5. Defina GITHUB_CLIENT_ID e GITHUB_CLIENT_SECRET abaixo (ou via Coolify env)
#
# Google OAuth:
#   1. console.cloud.google.com -> APIs & Services -> Credentials -> OAuth 2.0 Client IDs
#   2. Authorized redirect URI: https://ethitorial.panlabs.tech/api/auth/callback/google
#   3. Copie o Client ID e Client Secret
#
# Para ativar os botões no front, defina também:
#   NEXT_PUBLIC_GITHUB_OAUTH=1  (exibe botão GitHub)
#   NEXT_PUBLIC_GOOGLE_OAUTH=1  (exibe botão Google)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
NEXT_PUBLIC_GITHUB_OAUTH=
NEXT_PUBLIC_GOOGLE_OAUTH=
